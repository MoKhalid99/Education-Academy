@page "/register"
@using Education_Academy.Components.Layout
@using EducationAcademy.Models
@using Microsoft.AspNetCore.Identity
@layout EmptyLayout
@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager
@inject NavigationManager NavigationManager

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

<PageTitle>إنشاء حساب - Education Academy</PageTitle>

<div class="big_container">
    <div class="container">
        <div>
            <form action="account/register" method="post" class="form_container">
                <AntiforgeryToken />

                <input type="text" name="fullName" placeholder="الاسم الكامل" required>

                <input type="email" name="email" placeholder="الايميل" required>

                @* التحقق من رقم الهاتف المصري عبر Regex في المتصفح *@
                <input name="phone" type="tel" placeholder="رقم الهاتف (01x...)"
                       pattern="^(010|011|012|015)[0-9]{8}$" title="يرجى إدخال رقم هاتف مصري صحيح" required>

                <input type="password" name="password" placeholder="كلمة السر" required>

                <button type="submit" class="register_button">تسجيل الدخول</button>

                @if (!string.IsNullOrEmpty(errorMessage))
                {
                    <div class="validation-message">@errorMessage</div>
                }

                <div class="social-login-container">
                    <form action="account/ExternalLogin" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="returnUrl" value="/" />
                        <button type="submit" name="provider" value="Google" class="social-btn google-btn">
                            <i class="fab fa-google-plus-g"></i>
                        </button>
                    </form>
                    <form action="account/ExternalLogin" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="returnUrl" value="/" />
                        <button type="submit" name="provider" value="Facebook" class="social-btn facebook-btn">
                            <i class="fab fa-facebook-f"></i>
                        </button>
                    </form>
                    <form action="account/ExternalLogin" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="returnUrl" value="/" />
                        <button type="submit" name="provider" value="Twitter" class="social-btn twitter-btn">
                            <i class="fab fa-twitter"></i>
                        </button>
                    </form>
                    <form action="account/ExternalLogin" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="returnUrl" value="/" />
                        <button type="submit" name="provider" value="LinkedIn" class="social-btn linkedin-btn">
                            <i class="fab fa-linkedin-in"></i>
                        </button>
                    </form>
                </div>

                <div class="login-redirect">
                    لديك حساب بالفعل؟
                    <a href="/login" class="redirect-link">سجل دخولك</a>
                </div>
            </form>
        </div>
    </div>
</div>

@code {
    private RegisterModel registerModel = new();
    private string? errorMessage;

    private async Task HandleRegister()
    {
        var user = new ApplicationUser
        {
            UserName = registerModel.Email,
            Email = registerModel.Email,
            FullName = registerModel.FullName,
            PhoneNumber = registerModel.Phone,
            XP = 0,
            CurrentLevel = 1
        };
        var result = await UserManager.CreateAsync(user, registerModel.Password);

        if (result.Succeeded)
        {
            await SignInManager.SignInAsync(user, isPersistent: true);
            NavigationManager.NavigateTo("/");
        }
        else
        {
            errorMessage = string.Join(", ", result.Errors.Select(e => e.Description));
        }
    }
}

<style>
    .big_container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        direction: rtl;
    }

    .container {
        display: flex;
        background-image: url("gp images/Frame 2377011.png");
        background-repeat: no-repeat;
        background-size: cover;
        background-position-y: bottom;
        border-top-left-radius: 60px;
        height: 627px;
        width: 781px;
        position: relative;
    }

    .form_container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        row-gap: 20px;
        height: 100%;
        position: absolute;
        left: 50%;
        bottom: 30px;
        transform: translateX(-50%);
    }

    input {
        width: 400px;
        height: 50px;
        border-radius: 30px;
        border: none;
        background-color: #F5F5F5;
        font-size: 16px;
        box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
        text-align: center;
        outline: none;
        transition: background 0.2s;
    }

        input:focus {
            background-color: #e8e8e8;
        }

    ::placeholder {
        text-align: center;
        color: #888;
    }

    .register_button {
        width: 200px;
        height: 50px;
        border-radius: 30px;
        border: none;
        background-color: white;
        color: #009999;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: 0.25s ease-in-out;
        margin-top: 5px;
    }

        .register_button:hover {
            background-color: #48666D;
            color: white;
            border: 1px solid white;
            transform: scale(1.1);
        }

    /* تنسيق الأيقونات الاجتماعية */
    .social-login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 30px;
    }

    .social-btn {
        background: none;
        border: none;
        cursor: pointer;
        transition: transform 0.2s ease-in-out;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .social-btn:hover {
            transform: scale(1.15);
        }

    .google-btn {
        color: #EA4335;
        font-size: 38px;
    }

    .facebook-btn i {
        background-color: #3B5998;
        color: white;
        border-radius: 50%;
        width: 42px;
        height: 42px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
    }

    .twitter-btn {
        color: #1DA1F2;
        font-size: 40px;
    }

    .linkedin-btn {
        color: #0077B5;
        font-size: 40px;
    }

    /* رسائل الخطأ والتوجيه */
    .validation-message {
        color: #d9534f;
        font-size: 0.9rem;
        text-align: center;
        background-color: rgba(255, 255, 255, 0.8);
        padding: 5px 10px;
        border-radius: 10px;
    }

    .login-redirect {
        color: #333;
        font-size: 0.9rem;
        margin-top: 5px;
        text-align: center;
        background-color: rgba(255, 255, 255, 0.7);
        padding: 5px 15px;
        border-radius: 15px;
    }

    .redirect-link {
        color: #009999;
        font-weight: bold;
        text-decoration: underline;
        transition: opacity 0.2s;
    }

        .redirect-link:hover {
            opacity: 0.8;
        }
</style>